
#include <stdio.h>

/**
  * main - Prints the alphabet at reverse
  *
  * Return: Always (Success)
  */
int main(void)
{
    int c;
for (c = (int)z; c >= (int)a; c--)
    {
        putchar(c);
    }

    putchar((int)n);
return (0);
}
